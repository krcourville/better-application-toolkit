{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env", "**/.env.local"],
  "globalEnv": ["LOG_LEVEL", "DEBUG", "PORT", "LOCAL_DEV"],
  "tasks": {
    "build": {
      "description": "Build package or app",
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**"],
      "outputLogs": "new-only",
      "cache": true
    },
    "typecheck": {
      "description": "Type check package or app",
      "outputs": [],
      "cache": true,
      "outputLogs": "new-only"
    },
    "lint": {
      "description": "Lint package or app",
      "outputs": [],
      "cache": false
    },
    "dev": {
      "description": "Start development server",
      "cache": false,
      "persistent": true
    },
    "dev:async-local": {
      "description": "Watch-build @batkit/logger/async-local (Node subpath)",
      "cache": false,
      "persistent": true
    },
    "@batkit/logger#dev": {
      "description": "Logger has two tsup entry points; Turbo runs both watchers",
      "cache": false,
      "persistent": true,
      "with": ["@batkit/logger#dev:async-local"]
    },
    "test": {
      "description": "Run tests",
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "cache": true,
      "outputLogs": "new-only"
    },
    "clean": {
      "description": "Clean build artifacts",
      "cache": false
    }
  }
}
