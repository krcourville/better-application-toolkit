---
"@batkit/logger": patch
---

Internal cleanup only, no behavior change: merged two overlapping `LogMethod` overloads, converted a constructor parameter property to an explicit class field, and renamed some single-letter identifiers for lint compliance (`no-magic-numbers`, `id-length`, `func-style`).
