---
"@batkit/rfc9457": patch
---

Internal cleanup only, no behavior change: extracted the HTTP status range bounds (100/599) into named constants and renamed a generic type parameter for lint compliance (`no-magic-numbers`, `id-length`).
